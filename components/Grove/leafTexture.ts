// (deprecated) — the project moved to vertex-color shading inside
// leafGeometry.ts. Kept as an empty module so any stray import won't
// break the build. If you want to bring textures back later, the old
// implementation lives in git history.
export {};
